import React from 'react'

import { PrimaryButton } from './primary-button'

const Meta = {
  title: 'ui/atoms/PrimaryButton',
}

export default Meta

export const Default = () => <PrimaryButton>Войти</PrimaryButton>
